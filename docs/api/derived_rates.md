# Derived Rates

::: phoscrosstalk.derived_rates

!!! note
    `k_act(t)` and `s_prod(t)` are derived rates, not optimised theta parameters. They should remain non-negative and aligned with the model protein order.